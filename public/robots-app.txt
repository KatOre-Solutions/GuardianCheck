# robots.txt for app.guardiancheck.co.za, the GuardianCheck application.
#
# Served in place of robots.txt on the app host (see vercel.json). Nothing here
# is indexed: every response carries `X-Robots-Tag: noindex, nofollow`. Pages
# are deliberately NOT disallowed, because a crawler that may not fetch a page
# never sees that noindex and can still index the bare URL from inbound links.
# The public site, its sitemap and llms.txt are at https://guardiancheck.co.za.

User-agent: *
Disallow: /api/
