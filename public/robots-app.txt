# robots.txt for guardiancheck.co.za, the GuardianCheck application.
#
# Served in place of robots.txt on the application host once the domain split
# (#14) is live: the app keeps this address, and the public site moves to
# https://www.guardiancheck.co.za, which carries the sitemap and llms.txt.
#
# Nothing here is indexed: every response on this host carries
# `X-Robots-Tag: noindex, nofollow` (see vercel.json). Pages are deliberately
# NOT disallowed, because a crawler that may not fetch a page never sees that
# noindex and can still index the bare URL from inbound links, of which this
# host has many: church QR posters, invitations and shared links.

User-agent: *
Disallow: /api/
